server {
    listen 8444 ssl;
    server_name discovery;

    ssl_certificate     /certs/tls.crt;
    ssl_certificate_key /certs/tls.key;

    location = /.well-known/pelican-configuration {
        alias /etc/nginx/html/discovery.json;
        default_type application/json;
    }
}
